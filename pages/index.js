export default function Home() {
  return (
    <main style={{padding: 24, fontFamily: "system-ui, Arial"}}>
      <h1>Welcome</h1>
      <p>Static assets work from <code>/public</code> (e.g. <code>/images/foo.png</code>).</p>
    </main>
  );
}
